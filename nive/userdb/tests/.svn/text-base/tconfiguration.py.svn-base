# -*- coding: utf-8 -*-

import time
import unittest

from Poolyx.definitions import *
from Poolyx.security import *
from Poolyx.helper import FormatConfTestFailure

from Poolyx.userdb import app, root, user




class TestConf(unittest.TestCase):

    def test_conf1(self):
        r=app.configuration.test()
        if not r:
            return
        print FormatConfTestFailure(r)
        self.assert_(False, "Configuration Error")

    def test_conf2(self):
        r=root.configuration.test()
        if not r:
            return
        print FormatConfTestFailure(r)
        self.assert_(False, "Configuration Error")

    def test_conf3(self):
        r=user.configuration.test()
        if not r:
            return
        print FormatConfTestFailure(r)
        self.assert_(False, "Configuration Error")



if __name__ == '__main__':
    unittest.main()
